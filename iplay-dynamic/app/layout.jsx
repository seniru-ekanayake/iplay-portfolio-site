export const metadata = {
  title: "iPLAY Dynamic",
  description: "Entertainment-driven marketing systems.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
